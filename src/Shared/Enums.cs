using System;

namespace AnimalShelter.Shared
{
    // Local enums for controller helpers — keep separate from domain enums
    public enum Role
    {
        User = 0,
        Admin = 1,
        Employee = 2
    }
}
